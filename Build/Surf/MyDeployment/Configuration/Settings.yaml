#                                                                        #
# Example Settings                                                       #
#                                                                        #
# This file contains settings for various parts of the application.      #
# Copy this file to Settings.yaml, and adjust as necessary.              #
#                                                                        #
# Please refer to the default settings file(s) or the manuals for        #
# possible configuration options.                                        #
#                                                                        #


TYPO3:
  Flow:
    log:
      systemLogger:
        backend: Apimenti\Log\Backend\ApimentiFileBackend
        logger: Apimenti\Log\Log\Logger
      securityLogger:
        backend: Apimenti\Log\Backend\ApimentiFileBackend
        logger: Apimenti\Log\Log\Logger
      sqlLogger:
        backend: Apimenti\Log\Backend\ApimentiFileBackend
        logger: Apimenti\Log\Log\Logger
    i18n:
      defaultLocale: pt_BR
    persistence:
      doctrine:
        sqlLogger: Apimenti\Log\Log\ApimentiSqlLogger

      # It is good practice to not specify user name and password of the database
      # connection in this global Settings.yaml file. Rather specify them in the
      # settings of the respective context (Production / Development ...).
      backendOptions:
        driver: 'pdo_mysql'
        dbname: 'agilize_dev' # adjust to your database name
        user: 'root'         # adjust to your database user
        password: 'Por1InMe-$4us' # adjust to your database password
        host: 'agilize.cxxygd9lrhrm.us-east-1.rds.amazonaws.com'    # adjust to your database host
  SwiftMailer:
    transport:
      type: 'Swift_SmtpTransport'
      options:
#        host: 'email-smtp.us-east-1.amazonaws.com'
        host: 'smtp.sendgrid.net'
        port: '465'
#        username: 'AKIAIA5ZT23V2NBBC5YQ'
        username: 'Apimenti'
#        password: 'Ahr8mL+ZadSBT/ZTRDptVLJIHtcFXo8cA5bjvbMtN9BC'
        password: 'Por1InMe-$4us'
        encryption: 'ssl'
        localDomain: 'dev.agilize.com.br'
#    i18n:
#      defaultLocale: pt-BR
  # On Windows, you might need to uncomment the following lines and specify
  # the location of the PHP binary manually.
  #  core:
  #    phpBinaryPathAndFilename: 'C:/path/to/php.exe'

Apimenti:
  Account:
    nfseContext: 'production'